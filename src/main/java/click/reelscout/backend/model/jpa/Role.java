package click.reelscout.backend.model.jpa;

/**
 * Enum representing user roles within the application.
 */
public enum Role {
    MEMBER,
    VERIFIED_MEMBER,
    MODERATOR,
    PRODUCTION_COMPANY,
    ADMIN
}
